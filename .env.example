# Local development environment variables
# Copy to .env and customize values

DB_NAME=weather_todo_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_SSL_REQUIRE=false

# Optional: full connection string override
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/weather_todo_db
